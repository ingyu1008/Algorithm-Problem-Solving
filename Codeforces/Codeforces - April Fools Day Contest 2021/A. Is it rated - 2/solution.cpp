#include <iostream>
#include <vector>
#include <algorithm>

int main(void)
{
    std::string s;
    while (std::cin >> s)
    {
        std::cin >> s;
        std::cin >> s;
        std::cout << "NO\n";
    }

    return 0;
}

#include <iostream>
#include <vector>
#include <algorithm>

int main(void)
{
    std::cin.tie(0);
    std::ios_base::sync_with_stdio(false);

    int T;
    std::cin >> T;
    while (T--)
    {
        int a, b, c, d;
        std::cin >> a >> b >> c >> d;

        std::cout << b << " " << c << " " << c << "\n";
    }

    return 0;
}